import time
from typing import List

try:
    import winsound  # Для Windows beep
except ImportError:
    winsound = None


def loading_animation(message: str = "Поиск") -> None:
    """
        Отображает простую анимацию загрузки с последовательным добавлением точек,
        имитируя процесс ожидания, и завершает выводом сообщения и звуковым сигналом.

        Назначение:
            Используется для создания визуальной обратной связи при выполнении поиска
            или любой операции, требующей времени. Повышает интерактивность консольного интерфейса.

        Аргументы:
            message (str): Строка, отображаемая перед точками (по умолчанию "Поиск").

        Логика выполнения:
            1. Цикл из 3 итераций (i от 0 до 2):
                - Создаются точки: "." * (i + 1)
                - Строка форматируется как: `message + точки + пробелы для выравнивания`
                - Вывод через `print(..., end="\r")` для перезаписи строки
                - Пауза 0.5 секунды через `time.sleep(0.5)`
            2. После цикла:
                - Выводится финальное сообщение "✅ Поиск завершён!"
                - Воспроизводится звуковой сигнал через `play_success_sound()`

        Зависимости:
            - `time.sleep` для задержки
            - `sys.stdout` не используется явно, но вывод реализуется через `print(..., end="\r")`
            - `play_success_sound()` вызывается по завершении

        Возвращаемое значение:
            None

        Пример:
            loading_animation("Загружаем данные")
            → Показывает "Загружаем данные." → "Загружаем данные.." → "Загружаем данные..."
               → "✅ Поиск завершён!" → воспроизводится звуковой сигнал
        """
    for i in range(3):
        dots = "." * (i + 1)
        print(f"{message}{dots}{' ' * (2 - i)}", end="\r")
        time.sleep(0.5)
    print("✅ Поиск завершён!")
    play_success_sound()


def list_loading(items: List[str]) -> None:
    """
        Пошагово отображает процесс обработки списка элементов с визуальной обратной связью.

        Назначение:
            Используется для показа активности при работе со списком — например, при парсинге, загрузке или фильтрации.
            Повышает интерактивность консольного интерфейса за счёт динамического вывода и звукового сопровождения.

        Аргументы:
            items (List[str]): Список строковых элементов, подлежащих обработке.

        Логика выполнения:
            1. Для каждого элемента `item` из списка:
                - Печатается строка вида "🔍 Обрабатываем: item", заменяя предыдущую через `end="\r"`.
                - Пауза 0.3 секунды (`time.sleep(0.3)`) — имитация обработки.
            2. После завершения цикла:
                - Выводится сообщение "✅ Обработка завершена!"
                - Воспроизводится звуковой сигнал успеха (`play_success_sound()`).

        Возвращаемое значение:
            None

        Пример:
            list_loading(["Matrix", "Inception", "Interstellar"])
            → Последовательно отображаются строки "Обрабатываем: ..." с задержкой, затем финальный статус.
        """
    for item in items:
        print(f"🔍 Обрабатываем: {item}", end="\r")
        time.sleep(0.3)
    print("✅ Обработка завершена!")
    play_success_sound()


def highlight_text(text: str) -> None:
    """
        Визуально выделяет строку, обрамляя её декоративной рамкой из символов = и |.

        Назначение:
            Улучшает читаемость ключевых сообщений, делая их центром внимания в консольном выводе.
            Используется для заголовков, уведомлений или акцентных фраз.

        Аргументы:
            text (str): Строка, которую необходимо выделить.

        Логика выполнения:
            1. Вычисляется длина рамки: `len(text) + 4`, чтобы учесть пробелы и вертикальные линии.
            2. Формируется строка-рамка из символов `=`.
            3. Вывод по строкам:
                - Верхняя граница.
                - Строка с текстом, обрамлённым: "| text |".
                - Нижняя граница.

        Побочные эффекты:
            - Вывод в stdout: три строки с рамкой и содержимым.

        Возвращаемое значение:
            None

        Пример:
            highlight_text("Результаты поиска")
            →
            ==========================
            | Результаты поиска      |
            ==========================
        """
    border = "=" * (len(text) + 4)
    print(border)
    print(f"| {text} |")
    print(border)


def celebrate() -> None:
    """
        Выводит праздничную анимацию с эмодзи-фейерверками и сопровождающим звуком успеха.

        Назначение:
            Используется для позитивной обратной связи при завершении успешной операции — например, после вывода результатов поиска.
            Создаёт ощущение достижения, оживляя консольный интерфейс.

        Логика выполнения:
            1. Выводит вступительное сообщение: "🎉 Ура! Всё получилось!".
            2. Отображает последовательность из 6 эмодзи, циклически выбирая символы из списка `fireworks = ["🎆", "✨", "🔥", "💥"]`.
                - Каждое эмодзи выводится с паузой 0.2 секунды (`time.sleep(0.2)`), имитируя фейерверк.
            3. После завершения цикла вызывается `play_success_sound()`, который играет звуковой сигнал (если доступен).

        Побочные эффекты:
            - Вывод в stdout с эмодзи-анимацией.
            - Звуковое сопровождение (через winsound или текстовую заглушку).

        Возвращаемое значение:
            None

        Пример:
            celebrate()
            → Выводит "🎉 Ура! Всё получилось!", за ним — фейерверки и финальный звуковой сигнал.
        """
    fireworks = ["🎆", "✨", "🔥", "💥"]
    print("🎉 Ура! Всё получилось!", end=" ")
    for i in range(6):
        print(fireworks[i % len(fireworks)], end=" ")
        time.sleep(0.2)
    print()
    play_success_sound()


def print_error(message: str) -> None:
    """
        Выводит сообщение об ошибке с соответствующим визуальным и аудио-оформлением.

        Назначение:
            Используется для уведомления пользователя о возникновении ошибки,
            некорректном вводе или сбое в процессе выполнения.
            Подчёркивает отрицательный статус операции через иконку ⚠ и звуковой сигнал.

        Аргументы:
            message (str): Текст сообщения об ошибке, отображаемый пользователю.

        Логика выполнения:
            1. Форматированный вывод в консоль: "⚠ <message>".
            2. Воспроизведение звукового сигнала ошибки через `play_error_sound()`:
                - На Windows: звуковой сигнал через `winsound.Beep(400, 300)`
                - В остальных случаях: текстовая заглушка "(звук: ⚠ beep)"

        Побочные эффекты:
            - Вывод в stdout с пометкой ошибки.
            - Звуковое или текстовое сопровождение, в зависимости от платформы.

        Возвращаемое значение:
            None

        Пример:
            print_error("Неверный формат года")
            → ⚠ Неверный формат года
               (и звуковой сигнал, если доступен)
        """
    print(f"⚠ {message}")
    play_error_sound()


def print_success(message: str) -> None:
    """
        Выводит сообщение об успешной операции с визуальным и аудио-сопровождением.

        Назначение:
            Используется для подтверждения успешного выполнения действия — например, завершения поиска,
            обработки данных или сохранения состояния. Помечает результат как положительный с помощью эмодзи ✅ и звукового сигнала.

        Аргументы:
            message (str): Текст сообщения, информирующий пользователя об успешном результате.

        Логика выполнения:
            1. Форматированный вывод строки вида: "✅ <message>" в stdout.
            2. Вызов `play_success_sound()` для воспроизведения подтверждающего сигнала:
                - Если модуль `winsound` доступен (Windows), проигрывается beep.
                - В других случаях — текстовая заглушка "(звук: ✅ beep)" выводится в консоль.

        Побочные эффекты:
            - Текстовый вывод в консоль.
            - Звуковая или имитированная обратная связь.

        Возвращаемое значение:
            None

        Пример:
            print_success("Поиск завершён успешно")
            → ✅ Поиск завершён успешно
               (с последующим звуковым сигналом)
        """
    print(f"✅ {message}")
    play_success_sound()


def play_success_sound() -> None:
    """
        Воспроизводит сигнал успешной операции, если доступна поддержка звука.

        Назначение:
            Используется для аудиофидбэка в консольном интерфейсе, подтверждающего успешное выполнение действия.
            Работает на системах с поддержкой `winsound` (Windows), либо заменяется на текстовую заглушку.

        Логика выполнения:
            1. Проверка доступности модуля `winsound`:
                - Если модуль импортирован — вызывается `winsound.Beep(1200, 150)`:
                    • частота: 1200 Гц
                    • продолжительность: 150 мс
                - Если `winsound` отсутствует (например, не Windows):
                    • Вывод текстовой строки-заглушки "(звук: ✅ beep)"

        Зависимости:
            - winsound: модуль Windows API для воспроизведения сигнала (опционален).
            - print: используется для вывода заглушки при отсутствии звука.

        Побочные эффекты:
            - Аудиовывод при наличии `winsound`.
            - Текстовый вывод при отсутствии поддержки звука.

        Возвращаемое значение:
            None

        Пример:
            play_success_sound()
            → Воспроизводит звуковой сигнал или выводит заглушку, подтверждая успех операции.
        """
    if winsound:
        winsound.Beep(1200, 150)
    else:
        print("(звук: ✅ beep)")


def play_error_sound() -> None:
    """
        Воспроизводит звуковой сигнал ошибки, если поддерживается платформа, либо выводит текстовую заглушку.

        Назначение:
            Применяется для аудиофидбэка при возникновении ошибок, предупреждений или сбойных сценариев в консольном интерфейсе.
            Предупреждает пользователя о нежелательном исходе действия — визуально и звуком.

        Логика выполнения:
            1. Проверка наличия модуля `winsound`:
                - Если доступен (Windows):
                    • вызывается `winsound.Beep(400, 300)`
                        ◦ частота: 400 Гц
                        ◦ длительность: 300 мс
                - Иначе:
                    • выводится строка-заглушка "(звук: ⚠ beep)" — имитация сигнала ошибки

        Зависимости:
            - winsound: используется для системного звука (только Windows)
            - print: вывод заглушки при отсутствии звука

        Побочные эффекты:
            - Звуковой сигнал или текст в stdout

        Возвращаемое значение:
            None

        Пример:
            play_error_sound()
            → Воспроизводит предупреждающий сигнал (или текст), сопровождая сообщение об ошибке.
        """
    if winsound:
        winsound.Beep(400, 300)
    else:
        print("(звук: ⚠ beep)")
